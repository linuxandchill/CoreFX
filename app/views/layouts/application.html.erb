<!DOCTYPE html>
<html>
  <head>
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:site" content="@forexfax" />
    <meta name="twitter:title" content="Forex Fax" />
    <meta name="twitter:description" content="Forex Fax - Facts & strategy." />
    <meta name="twitter:image" content="https://i.imgur.com/YYwuep0.png" />

    <title>Forex Fax - Pro forex facts & strategy</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "ForexFax - Pro forex facts & strategy" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "ForexFax - Forex facts Learn Forex" %>">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
    <%= favicon_link_tag 'favicon.ico' %>

  </head>

  <body>
    <!-- Static navbar -->
    <nav class="navbar navbar-default navbar-fixed-top">
      <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">ForexFax</a>
      </div>
      <div id="navbar" class="navbar-collapse collapse">
             <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? && current_user.has_role?(:pro_user) %>
            <li> <%= link_to "Welcome!", root_path %></li>
          <% else %>
            <li class=""><a class="btn btn-primary navbar-btn go-pro-btn" type="button" href="/pro"><span class="glyphicon glyphicon-flash" id=""></span> Go Pro</a></li>
          <% end %>
          <% if user_signed_in? %>
            <li> <%= link_to "Logout", destroy_user_session_path, method: :delete %> </li>
          <% else %>
            <li> <%= link_to "Login", new_user_session_path %> </li>
          <% end %>
        </ul>
      </div><!--/.nav-collapse -->
      </div><!--/.nav-collapse -->
    </nav>
    <% flash.each do |name, msg| %>
			<%= content_tag(:div, msg, class: "alert alert-info") %>
		<% end %>
    <%= yield %>
  </body>
    <footer class="footer">
      <div class="container">
        <span class="text-muted">Copyright &#169; 2017 ForexFax // <a href="/disclaimer">Risk Disclaimer</a></span>
      </div>
    </footer>
</html>
